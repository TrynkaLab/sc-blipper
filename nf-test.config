// nf-test runner configuration.
//
// Keep this file limited to nf-test settings. Nextflow runtime overrides for
// tests live under tests/nextflow/config/<category>/*.config so production
// profiles under conf/ stay untouched and tests/ can also host other test
// systems such as pytest.
config {
    testsDir "tests/nextflow"
    workDir "tests/.cache/nf-test"
}
